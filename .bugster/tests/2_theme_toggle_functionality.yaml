name: Theme toggle functionality
page: /
page_path: app/page.tsx
task: Test the dark/light theme toggle functionality
steps:
- Navigate to the homepage
- Observe the current theme
- Click on the theme toggle button in the navigation
- Verify the theme changes (background and text colors should update)
expected_result: Theme should switch between dark and light mode when toggled
