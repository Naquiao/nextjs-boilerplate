name: Font Loading Test
page: /
page_path: app/page.tsx
task: Verify the custom fonts (Geist and Geist Mono) load correctly
steps:
- Navigate to the homepage ('/')
- Inspect text elements on the page
- Verify text is rendered with the Geist font family
- Verify any monospace text is rendered with the Geist Mono font family
expected_result: Text elements should display using the Geist and Geist Mono fonts
