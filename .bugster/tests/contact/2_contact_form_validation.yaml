name: Contact form validation
page: /contact
page_path: app/contact/page.tsx
task: Verify contact form validation for required fields
steps:
- Navigate to the Contact page
- Leave all fields empty
- Click the Submit button
- Verify validation error messages are displayed
- Fill in one required field
- Click Submit again
- Verify validation errors still appear for empty fields
expected_result: Form shows validation errors when submitted with empty fields
