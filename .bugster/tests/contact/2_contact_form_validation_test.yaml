name: Contact form validation test
page: /contact
page_path: app/contact/page.tsx
task: Verify contact form validation
steps:
- Navigate to the contact page
- Leave required fields empty
- Click the submit button
- Verify error messages appear for empty required fields
- Enter invalid email format
- Verify email format error message appears
expected_result: Form should display error messages for invalid or empty inputs
